import {stringify,INVALID as INVALID_POINT} from "../common/TimePoint";
import {Fragment} from "react"


const Timer = (props:any) => {
    return (<Fragment>{stringify(INVALID_POINT)}</Fragment>)
}

export default Timer;